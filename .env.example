# Model selection — change this to hot-swap providers.
# Format: "<provider>:<model-name>" via LangChain's init_chat_model.
# Examples:
#   openai:gpt-4o
#   openai:gpt-4.1-mini
#   google_genai:gemini-2.0-flash
AGENT_MODEL=openai:gpt-4o

# Provider API keys — only the active provider's key is required.
OPENAI_API_KEY=...
GOOGLE_API_KEY=...

# Observability
LANGSMITH_API_KEY=...
LANGSMITH_TRACING=true
LANGSMITH_PROJECT=agent-mvp

# Optional: Tavily for web search tool
TAVILY_API_KEY=...
